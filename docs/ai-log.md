# AI 使用日志（AI Usage Log）

> 本文件用于记录我在开发过程中与 AI 的关键交互，属于课程的 AI Compliance（10%）评分证据。
> 要求：完整、有时间顺序、能体现「提问 → 判断 → 验证 → 修改」的过程。

## 项目：个人主页

---

### 第 1 次交互 · 需求确认

- **时间**：2026-09-03
- **我的提问（prompt 摘要）**：
  - 请先理解我的课程大纲，并给出学习建议。
  - 我想用 Vibe Coding 做个人网站，是否就不用装代码编辑器了？
- **AI 输出摘要**：
  - 解读了课程大纲，指出课程重点是「用 AI 协作做出可运行、可解释、有过程证据的项目」。
  - 说明 Vibe Coding 不等于完全不用代码环境，仍需要能运行、预览、留版本记录的环境。
- **我的判断**：
  - 采纳「不装编辑器也可以，但要留过程证据」的建议。
  - 决定采用纯静态 HTML/CSS/JS，最易上手也最容易留 Git 记录。
- **后续修改**：无（此轮为需求澄清）。

---

### 第 2 次交互 · 需求问答

- **时间**：2026-09-03
- **我的提问（prompt 摘要）**：
  - 让我回答几个问题来确认主页需求。
- **AI 输出摘要**：
  - 提出 5 个结构化问题：视觉风格、核心板块、顶部标题、身份信息、实现方式。
- **我的回答（关键决策）**：
  1. 视觉风格：精美、fancy、少女风。
  2. 板块：关于我、学习与课程、项目作品、成长时间线、联系方式。
  3. 顶部标题：用真实姓名（暂用占位名「小满」）。
  4. 身份信息：天津大学（深圳）· 计算机科学与技术 · 2026 级大一。
  5. 实现方式：由 AI 决定 → 纯静态 HTML/CSS/JS。
- **我的判断**：
  - 接受 AI 的纯静态方案，因为部署简单、便于留版本记录。
- **后续修改**：已完成联系方式（邮箱 987341910@qq.com、电话/微信 18135204213），并删除 GitHub。

---

### 第 3 次交互 · 生成代码

- **时间**：2026-09-03
- **我的提问（prompt 摘要）**：
  - 帮我生成个人主页，采用少女风视觉，包含上述 5 个板块，并留存版本记录。
- **AI 输出摘要**：
  - 生成 `index.html`、`css/style.css`、`js/main.js` 三个文件。
- **我的判断与验证**：
  - 检查了目录结构，确认样式与脚本路径引用正确。
  - 确认移动端菜单、滚动动画、页脚年份三个交互逻辑清晰、有注释。
- **后续修改**：
  - 补充了 `.reveal`/`.visible` 的 CSS 动画类（初版缺失，我指出后 AI 补上）。

---

### 第 4 次交互 · 补充个人信息 + 新增数字分身

- **时间**：2026-09-03
- **我的提问（prompt 摘要）**：
  - 提供真实个人信息：姓名「张景怡」、学校「天津大学香港理工大学深圳未来技术学院」、专业「计算机科学与技术」、兴趣「音乐（R&B）、羽毛球、社交」、特点「虎牙」。
  - 要求页面至少包含：头像、名字和一句话介绍、个人信息展示区、一个可提问的数字分身聊天区。
- **AI 输出摘要**：
  - 更新了所有板块的姓名和学校信息。
  - 新增「个人信息展示区」（姓名/介绍/身份/兴趣/特点）。
  - 新增「数字分身聊天区」，用关键词匹配实现简单问答。
- **我的判断**：
  - 接受「数字分身先用关键词匹配、暂不接入真实大模型」的方案，因为演示稳定、无需后端。
  - 接受 AI 为「一句话介绍」「别人最可能问的 3 个问题」填的暂用默认值，后续再替换成自己的版本。
- **验证**：
  - 待浏览器预览确认聊天区交互是否正常。

---

### V2.9 内容整理 · 删除项目区并统一音乐偏好

- **时间**：2026-09-19
- **我的提问（prompt 摘要）**：
  - 删除内容不足的「项目作品」板块；留言簿留到 V3 接入后端时再开发。
  - 将音乐偏好由「尤其 R&B」更新为「R&B and hiphop」，并同步相关信息。
- **AI 输出摘要**：
  - 删除项目板块、导航入口和未使用样式，调整首页按钮及后续板块编号。
  - 同步更新首页、个人信息、数字分身及项目说明中的音乐偏好和导航文案。
- **我的判断**：
  - V2.9 保持纯静态页面，不用 `localStorage` 制作容易被误解为公开留言的临时留言簿。
  - V3 再将留言簿放在联系方式前，并接入可持久化的后端存储。

---

### V3 访客反馈 · 后端设计与前端实现

- **时间**：2026-09-20
- **我的提问（prompt 摘要）**：
  - 阅读老师的 V3 课件，按照课程流程构建反馈数据库、访客反馈入口并准备发布到 GitHub Pages。
- **AI 输出摘要**：
  - 根据课件将 V3 范围限定为「匿名访客只提交反馈，站长在 Supabase 后台查看」，不公开读取全部反馈。
  - 新增夜间便签风格的反馈入口与无跳转弹窗，收集昵称、关系、设备和反馈正文。
  - 新增表单校验、字数提示、设备识别、提交中状态、防重复点击、隐藏诱捕字段及成功/失败提示。
  - 新增 `supabase/feedback.sql`，包含 `feedback` 表、RLS 和匿名 INSERT-only 策略。
  - 新增 `js/supabase-config.js` 占位配置，等待站长创建 Supabase 项目后填写 Project URL 与 publishable key。
- **安全判断**：
  - publishable key 可以进入浏览器代码，但安全边界必须由 RLS 提供。
  - secret/service_role key 和数据库密码不得写入前端或提交 GitHub。
  - 暂不开放匿名 SELECT、UPDATE、DELETE，也不制作公开留言墙。
- **待人工完成**：
  - 已创建 Supabase 项目并运行 SQL；Project URL 与 publishable key 已接入前端。
  - 匿名 INSERT 联调返回 `201 Created`，匿名 SELECT 验证返回 `401`，权限符合课程目标。
- **发布前决定**：
  - 站长确认继续公开现有联系方式，并保留两首完整本地音乐文件随网站发布。
  - 代码已推送至 GitHub，并从 `main` 分支根目录发布到 GitHub Pages。
  - 公网地址返回 `200`，页面标题与 V3 反馈入口检查通过。

### V3.1 手机端首轮反馈修正

- **反馈**：手机滑动拖尾卡顿、台灯难以点中、课程卡片点击没有反应。
- **定位**：触屏端仍运行粒子 Canvas；首页文字层覆盖台灯命中区域；部分手机模拟悬停导致课程点击状态被抵消。
- **修改**：先关闭移动端高成本桌面拖尾，随后根据站长希望保留效果的反馈，改为独立轻量拖尾（约 30 FPS、1 倍像素比、最多 10 点、无粒子与模糊）；非交互文案透传触摸并保留按钮点击；课程逻辑按真实 hover 能力区分桌面与触屏。
- **待验证**：重新发布后，在同一手机上复测滚动、台灯开关、三张课程卡和反馈提交。
- **二次修正**：首个轻量版在实机完全不可见，改用被动 `touchstart/touchmove` 持续获取原生滚动坐标，并提高轨迹不透明度、线宽与淡出时间，仍维持 30 FPS 和最多 10 点。
- **延迟修正**：实机滚动已流畅但拖尾稍有延迟，将绘制和触摸采样提高到约 50 FPS，同时将轨迹上限降为 8 点并缩短淡出，换取更贴手的反馈而不扩大总体绘制负担。
- **最终处理**：多轮手机实机测试后，触摸拖尾仍有明显延迟，因此移除移动端拖尾及其触摸监听；桌面端效果继续保留，页面滚动、台灯按钮和课程卡片的触控修复不变。
- **留言簿升级**：加入公开留言列表、访客公开授权、站长审核与回复展示。公开读取仅开放昵称、留言、日期和回复字段；关系、设备与未审核内容继续受 RLS 和字段权限保护，旧留言保持私密。
- **发布流程调整**：根据站长反馈，将访客主动勾选公开的留言改为自动发布；保留 `is_approved` 作为事后隐藏开关，未授权公开的历史留言不受影响。
- **即时绘制修正**：提高帧率后实机仍感到明显延迟，进一步取消“触摸后等待下一动画帧”的路径；每次有效 `touchmove` 直接绘制最新位置，轨迹缩为最多 6 点，动画帧仅承担淡出。

### V4 公开留言整理与移动端体验优化

- **时间**：2026-09-24 至 2026-09-26
- **我的提问（prompt 摘要）**：
  - 读取访客公开留言，按 Impact–Effort 原则整理 V4 改进项。
  - 优先修复手机端台灯遮挡或悬空、专辑纵向滑动冲突与卡顿，并统一课程卡片配色。
- **AI 输出摘要**：
  - 将反馈归纳为台灯布局、专辑手势与性能、个人照片、课程配色、日夜主题和作品内容等方向。
  - 建议 V4 聚焦高影响问题，暂缓日夜主题与作品板块；站长随后决定不展示虎牙照片。
  - 手机专辑改为左右手势，减少移动端可见叠层并保留按钮和键盘操作。
  - 课程卡片调整为与夜间工作室一致的深色体系。
  - 初版台灯碰撞规避会使灯体脱离背景桌面；根据实机截图再次判断，最终为手机端建立带木质桌沿的独立灯光舞台，桌面端保持原布局。
- **我的判断与验证**：
  - 采纳独立灯光舞台，因为它不依赖不同手机比例下的背景裁切，能同时避免遮挡和悬空。
  - 不采纳虎牙照片展示，保留个人隐私与当前插画式主页风格。
  - 已通过手机实机查看并确认 V4 可以发布。

---

## 使用规范自检

- [x] 关键交互有记录
- [ ] 标注 AI 生成代码（在代码注释中说明模块用途）
- [x] 记录了我对 AI 输出的判断与修改
- [ ] Demo Day 前能逐段解释代码（待做）
- [x] 未向 AI 提交任何敏感数据（密钥、密码、个人隐私）
